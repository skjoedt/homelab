---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qui
spec:
  ingressClassName: traefik
  rules:
    - host: qui.traefik.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: qui
                port:
                  name: http
