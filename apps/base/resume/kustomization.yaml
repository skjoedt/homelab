---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: resume

resources:
  - namespace.yaml
  - secrets.yaml
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - ingress.yaml
  - cluster.yaml
  - pod-monitor.yaml
  - object-store.yaml
  - scheduled-backup.yaml

patches:
  - path: cluster-backup-patch.yaml
