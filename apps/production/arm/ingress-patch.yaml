---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: arm
  namespace: arr
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - &host arm.fanen.dk
      secretName: arm-fanen-dk-tls
  rules:
    - host: *host
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: arm
                port:
                  name: http
