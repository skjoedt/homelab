---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: musicseerr
  namespace: arr
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - &host musicseerr.fanen.dk
      secretName: musicseerr-fanen-dk-tls
  rules:
    - host: *host
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: musicseerr
                port:
                  name: http
