# cloud-init network config for static IP
version: 2
ethernets:
  eth0:
    dhcp4: false
    addresses:
      - ${ip_address}/24
    gateway4: ${gateway}
    nameservers:
      addresses:
        - "8.8.8.8"
        - "8.8.4.4"
