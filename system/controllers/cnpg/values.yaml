---
cloudnative-pg:
  monitoring:
    podMonitorEnabled: false  # Deprecated; PodMonitor is created manually per cluster
    grafanaDashboard:
      create: false  # Dashboard is provisioned via monitoring/configs
